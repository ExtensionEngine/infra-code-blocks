export const appName = 'static-site-test';

export const baseDomain = `ss.${process.env.ICB_DOMAIN_NAME}`;

export const staticSiteName = `${appName}-default`;

export const staticSiteDomain = `dft.${baseDomain}`;
