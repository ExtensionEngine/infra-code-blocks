## 2.0.0
### Breaking Changes
- Provide the whole `vpc` object instead providing `vpcId`, `isolatedSubnetIds` and `vpcCidrBlock` individually
- `skipFinalSnapshot` replaced with `deleteAutomatedBackups`
- `allocatedStorage` type changed to `string`
